const config = {
  appId: "com.maxxforge.projectm",
  appName: "Project Paige",
  webDir: "dist",
  bundledWebRuntime: false,
  server: {
    androidScheme: "https"
  },
  android: {
    allowMixedContent: false
  }
};

export default config;
